# Environment variables for Docker Compose
# Copy this file to .env and fill in your values

# Google Gemini API Key - REQUIRED
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_google_api_key_here

# OpenRouter API Key (optional - for open-source models via OpenRouter)
# Get your API key from: https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Scaleway API Key (optional - for EU-hosted models via Scaleway)
# Get your API key from: https://console.scaleway.com/iam/api-keys
VITE_SCALEWAY_API_KEY=your_scaleway_api_key_here
# Optional: override base URL for project-scoped endpoints
# VITE_SCALEWAY_BASE_URL=https://api.scaleway.ai/v1

# Backend configuration
PYTHONUNBUFFERED=1

# API URL for frontend to connect to backend
# Keep as localhost:8000 — the browser always reaches the backend via the exposed host port.
# (http://backend:8000 only works for server-side requests, not browser fetches.)
VITE_API_URL=http://localhost:8000

# Environment
NODE_ENV=production
